<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Create Bid</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/styleSheets/font.css">
</head>

<body class="bg-gradient-to-br from-blue-900 to-black min-h-screen text-white flex flex-col items-center justify-center px-4">
  <div class="bg-white text-black rounded-2xl shadow-lg max-w-md w-full p-6">
    <h1 class="text-3xl font-bold mb-6 text-center">Place Your Bid</h1>
    
    <form action="/Main/create-bid" method="POST" class="space-y-5">
      <div>
        <label for="color" class="block text-sm font-semibold mb-1">Choose Color</label>
        <select name="color" id="color" required class="w-full border border-gray-300 rounded-lg px-3 py-2">
          <option value="" disabled selected>Select a color</option>
          <option value="red">Red</option>
          <option value="blue">Blue</option>
        </select>
      </div>

      <div>
        <label for="amount" class="block text-sm font-semibold mb-1">Bid Amount (₹)</label>
        <input type="number" name="amount" id="amount" min="1" required class="w-full border border-gray-300 rounded-lg px-3 py-2" placeholder="Enter amount" />
      </div>

      <div>
        <label class="block text-sm font-semibold mb-1">Bid Time</label>
        <p class="px-3 py-2 bg-zinc-100 rounded-lg"><%= new Date().toLocaleString() %></p>
      </div>

      <div>
        <label for="resultTime" class="block text-sm font-semibold mb-1">Result Time (e.g., 12:00 AM)</label>
        <input type="text" name="resultTime" id="resultTime" required placeholder="Enter result time"
          class="w-full border border-gray-300 rounded-lg px-3 py-2" />
      </div>

      <button type="submit"
        class="w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 rounded-lg transition">Submit Bid</button>
    </form>
  </div>
</body>

</html>
